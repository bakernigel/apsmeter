# Describes the format for available remote services


get_aps_data:
  target:
    entity:
      domain: sensor
  fields:
    command:
      required: false
      example: "date"
      selector:
        object:      
        
